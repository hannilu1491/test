import Image from 'next/image';import { Project } from '@/types/project';
export default function CaseStudyHero({project}:{project:Project}){return <section className="mb-16"><div className="relative h-[55vh] min-h-[360px] overflow-hidden rounded-b-3xl"><Image src={project.heroImage} alt={project.title} fill className="object-cover" priority/></div><div className="mx-auto mt-8 max-w-5xl px-6"><p className="text-xs uppercase tracking-[0.2em] text-caramel">{project.category} · {project.year}</p><h1 className="mt-2 font-serif text-5xl">{project.title}</h1><p className="mt-2 text-warm-700">{project.role}</p></div></section>}
